{
	"name": "customerbase",
	"version": "1.0.0",
	"description": "simmple graphql server",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev:server": "nodemon server.js",
		"json:server": "json-server --watch data.json"
	},
	"author": "Waqqar Suleman",
	"license": "MIT",
	"dependencies": {
		"axios": "^0.19.2",
		"express": "^4.17.1",
		"express-graphql": "^0.9.0",
		"graphql": "^15.0.0",
		"json-server": "^0.16.1"
	},
	"devDependencies": {
		"nodemon": "^2.0.4"
	}
}
